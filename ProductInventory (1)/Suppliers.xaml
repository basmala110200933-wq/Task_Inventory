<Window x:Class="ProductInventory.Suppliers"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProductInventory"
        mc:Ignorable="d"
        Title="Suppliers" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Supplier Management" 
                   FontSize="22" FontWeight="Bold"
                   Foreground="DarkSlateBlue"
                   HorizontalAlignment="Center" Margin="0,10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15" Grid.Row="1">
                    <Button Content="Add Supplier" Width="130" Margin="5" Background="#FFB3E5FC" Click="Add_Click"/>
                    <Button Content="Edit Supplier" Width="130" Margin="5" Background="#FFC5CAE9" Click="Edit_Click"/>
                    <Button Content="Delete Supplier" Width="130" Margin="5" Background="#FFFFCDD2" Click="Delete_Click"/>
                </StackPanel>

                <DataGrid x:Name="SuppliersGrid" Grid.Row="2" Margin="10"
                  AutoGenerateColumns="False"
                  FontSize="14" FontFamily="Comic Sans MS">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding supid}" Width="80"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding SName}" Width="150"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="150"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
</Window>
